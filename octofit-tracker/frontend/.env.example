# Required for GitHub Codespaces API requests.
# Copy this file to .env.local and set the value to your Codespace name.
VITE_CODESPACE_NAME=my-codespace

# If VITE_CODESPACE_NAME is left unset, the app falls back to http://localhost:8000.
